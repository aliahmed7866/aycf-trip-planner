const CACHE="aycf-pwa-v1";const ASSETS=["/static/pwa.js","/static/icon-192.png","/static/icon-512.png"];self.addEventListener("install",event=>{event.waitUntil(caches.open(CACHE).then(cache=>cache.addAll(ASSETS)));self.skipWaiting();});self.addEventListener("activate",event=>event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(key=>key!==CACHE).map(key=>caches.delete(key)))).then(()=>self.clients.claim())));self.addEventListener("fetch",event=>{if(event.request.method!=="GET")return;const url=new URL(event.request.url);if(url.origin!==self.location.origin)return;if(event.request.mode==="navigate"){event.respondWith(fetch(event.request).catch(()=>new Response("<!doctype html><meta name=viewport content='width=device-width'><meta name=theme-color content='#070b14'><style>body{font:18px system-ui;background:#070b14;color:#fff;padding:12vh 24px;text-align:center}p{color:#94a0b2}</style><h1>AYCF is offline</h1><p>Start AYCF from the Admin Hub, then reopen the app.</p>",{headers:{"Content-Type":"text/html"}})));return;}if(url.pathname.startsWith("/static/"))event.respondWith(fetch(event.request).then(response=>{if(response.ok){const copy=response.clone();caches.open(CACHE).then(cache=>cache.put(event.request,copy));}return response;}).catch(()=>caches.match(event.request)));});